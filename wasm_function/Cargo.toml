[package]
name = "wasm_function"
version = "0.1.0"
edition = "2021"
license = "MIT"

[lib]
crate-type = ["cdylib"]

[dependencies]
arrow = { workspace = true }
wasm_udf = { path = "../wasm_udf"}

[dev-dependencies]
wasm-bindgen-test = "0.3"
